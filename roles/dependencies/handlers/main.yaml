---

- name: Restart MariaDB
  become: true
  systemd:
    state: restarted
    name: mariadb

- name: Enable MariaDB
  become: true
  systemd:
    state: enabled
    name: mariadb

- name: Yum clean all
  become: true
  command: Clear all cached info 
