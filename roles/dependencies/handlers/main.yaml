---

- name: Restart MariaDB
  become: true
  systemd:
    state: restarted
    name: mariadb

- name: Enable MariaDB
  become: true
  systemd:
    state: enabled
    name: mariadb

- name: Clear all cached info
  become: true
  command: yum clean all

